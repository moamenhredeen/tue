import type { JSX, ParentProps } from 'solid-js'
import Layout from '../components/Layout'

export default function Settings(props: ParentProps): JSX.Element {
	return (
		<Layout>
			<h1>Settings Page</h1>
		</Layout>
	)

}
