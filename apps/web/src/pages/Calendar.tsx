import type { JSX, ParentProps, VoidProps } from 'solid-js'
import Layout from '../components/Layout'


export default function Calendar(props: ParentProps): JSX.Element  {
	return (
		<Layout title="Home">
		</Layout>
	)
}
