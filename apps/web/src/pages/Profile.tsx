import type { JSX, ParentProps } from 'solid-js'
import Layout from '../components/Layout'

export default function Profile(props: ParentProps): JSX.Element {
	return (
		<Layout>
			<h1>Profile Page</h1>
		</Layout>
	)

}
